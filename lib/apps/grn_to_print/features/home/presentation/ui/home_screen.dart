import 'package:flutter/material.dart';

class GrnToPrintHomeScreen extends StatelessWidget {
  const GrnToPrintHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GRN to Print'),
      ),
    );
  }
}

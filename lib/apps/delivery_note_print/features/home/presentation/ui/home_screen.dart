import 'package:flutter/material.dart';

class DeliveryNotePrintHomeScreen extends StatelessWidget {
  const DeliveryNotePrintHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Delivery Note Print'),
      ),
    );
  }
}

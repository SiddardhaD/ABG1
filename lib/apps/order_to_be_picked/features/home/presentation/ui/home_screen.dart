import 'package:flutter/material.dart';

class OrderToBePickedHomeScreen extends StatelessWidget {
  const OrderToBePickedHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Order to be Picked'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class PoToReceiveHomeScreen extends StatelessWidget {
  const PoToReceiveHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PO to Receive'),
      ),
    );
  }
}

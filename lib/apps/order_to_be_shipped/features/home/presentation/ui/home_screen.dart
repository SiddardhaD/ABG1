import 'package:flutter/material.dart';

class OrderToBeShippedHomeScreen extends StatelessWidget {
  const OrderToBeShippedHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Order to be Shipped'),
      ),
    );
  }
}
